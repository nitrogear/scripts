# Generated by Chef for <%= node[:fqdn] %>
# Cluster host

    <% @renders.each do |render| %>
      /<%= render[:name] %>
       
        {
          /hostname "<%= render[:hostname] %>"
          /port "<%= render[:port] %>"
          /timeout "<%= render[:timeout] %>"
        }
    <% end %>
